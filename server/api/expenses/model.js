'use strict';

// var path = require('path');


module.exports = function (modelId) {
    // var basePath = path.join(process.cwd(), 'data', 'json');

    // var HELPERS = require(path.join(basePath, 'model-000-helpers.json'));
    // var DEFS_FACTORY = require(path.join(basePath, 'model-101-defs-factory.json'));
    // var STATIC_SERVER = require(path.join(basePath, 'model-101-static-server.json'));
    // var SCENARIOS = require(path.join(basePath, 'model-201-scenarios.json'));
    // // var PLAN = require(path.join(basePath, 'model-301-plan.json'));
    // var SCHEDULE = require(path.join(basePath, 'model-401-schedule.json'));

    // var output = {};
    // output.helpers = HELPERS;
    // output.defs_factory = DEFS_FACTORY;
    // output.static_server = STATIC_SERVER;
    // output.scenarios = SCENARIOS;
    // // output.plan = PLAN;
    // output.schedule = SCHEDULE;

    return 'Hello';
};
